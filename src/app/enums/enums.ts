import { Environments } from "./environments.enum";
import { ErrorSeverityLevel } from "./error-severity-level.enum";
import { WordType } from "./word-types.enum";

export class Enums {
  static WordTypes = WordType;
  static ErrorSeverityLevel = ErrorSeverityLevel;
  static Environments = Environments;
}
